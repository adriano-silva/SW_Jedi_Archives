<!DOCTYPE html>
<html lang="en">
<body class="text-center">
<div class="container">
  <%= image_tag 'Star_Wars_logo-1.png', class: 'mb-4', width: '450', height: '200' %>


  <h1 class="h3 mb-3 font-weight-normal"><%= t('signup') %></h1>

  <%= form_for @user, :html => {:class => 'form-signin'} do |f| %>
    <div class="form-group">
      <%= f.label(:username, {:class => 'sr-only'}) %>
      <%= f.text_field(:username, {:class => 'form-control', :placeholder => t('username')}) %>
    </div>
    <div class="form-group">
      <%= f.label(:password, {:class => 'sr-only'}) %>
      <%= f.password_field(:password, {:class => 'form-control', :placeholder => t('password')}) %>
    </div>
    <%= f.submit(t('createUser'), {:class => 'btn btn-primary btn-lg btn-block'}) %>
  <% end %>

  <p class="mt-5 mb-3 text-muted"><%= t('copyright') %></p>
</div>
</body>
</html>